(
    define (split start end)
    (
        if (= n 0)
            painter
            (
                let
                    ((smaller ((split start end) painter (- n 1))))
                    (start painter (end smaller smaller))
            )
    )
)
